{
  "build": {
    "command": "pip install -r requirements.txt && python -m flask run",
    "output_directory": "dist",
    "root_dir": "/"
  },
  "routes": [
    { "src": "/*", "dest": "/functions/_worker.js" }
  ],
  "env": {
    "FLASK_APP": "app.py",
    "FLASK_ENV": "production"
  }
}
