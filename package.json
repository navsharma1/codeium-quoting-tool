{
  "name": "codeium-quoting-tool",
  "version": "1.0.0",
  "description": "Salesforce Quoting Tool",
  "main": "functions/_worker.js",
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy .",
    "build": "mkdir -p dist && cp -r static/* dist/ 2>/dev/null || true"
  },
  "dependencies": {
    "hono": "^3.11.7",
    "jsforce": "^1.11.1"
  },
  "devDependencies": {
    "wrangler": "^3.22.1"
  }
}
